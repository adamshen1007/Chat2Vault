# Synthetic ChatGPT fixtures

Every fixture in this directory is invented test data. No user export or real conversation content is included.

JSON files exercise supported collection and single-conversation shapes. Files ending in `.fixture.json` describe ZIP entries assembled in memory by the test helper so hostile paths and archive limits can be tested without extracting files to disk.
